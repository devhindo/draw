# draw

A locally hosted `tldraw` board to draw diagrams locally, built with Vite and React.
Everything runs locally, including the web fonts.

## Getting Started

1. Install dependencies
```bash
npm install
```

2. Run the development server
```bash
npm run dev
```

3. Build for production
```bash
npm run build
```
